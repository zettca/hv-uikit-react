import { User } from "@phosphor-icons/react/User";
import {
  iconContainerClasses as classes,
  HvIconContainer,
} from "@hitachivantara/uikit-react-core";

import { Playground } from "../../components/code/Playground";
import { Header } from "../../components/Header";
import { getComponentData } from "../../utils/component";

<Header params={{ name: "IconContainer", classes }} />

<Playground
  Component={HvIconContainer}
  componentName="HvIconContainer"
  controls={{
    color: { type: "color", defaultValue: "warning" },
    size: {
      type: "slider",
      defaultValue: "md",
      values: ["xs", "sm", "md", "lg", "xl"],
    },
    rotate: { defaultValue: false },
  }}
>
  <User />
</Playground>
