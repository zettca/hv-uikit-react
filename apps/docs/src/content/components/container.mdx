import {
  containerClasses as classes,
  HvContainer,
  HvPanel,
} from "@hitachivantara/uikit-react-core";

import { getComponentData } from "../../utils/component";

import { Playground } from "../../components/code/Playground";
import { Header } from "../../components/Header";

<Header params={{ name: "Container", classes }} />

<Playground
  Component={HvContainer}
  componentName="HvContainer"
  controls={{
    maxWidth: { defaultValue: "sm" },
    fixed: { defaultValue: false },
    disableGutters: { defaultValue: false },
  }}
>
  <HvPanel className="h-100px">Content</HvPanel>
</Playground>
